import {expect,it} from 'vitest';
import {memoryPrivateState,witnesses} from './private-state';
it('scopes in-memory state by contract and clears it',async()=>{const p=memoryPrivateState();p.setContractAddress('a');await p.set('x',{});p.setContractAddress('b');expect(await p.get('x')).toBeNull();await p.set('x',{});await p.remove('x');expect(await p.get('x')).toBeNull();await p.clear();p.setContractAddress('a');expect(await p.get('x')).toBeNull();await expect(p.exportPrivateStates()).rejects.toThrow();await expect(p.importPrivateStates({} as never)).rejects.toThrow();await expect(p.exportSigningKeys()).rejects.toThrow();await expect(p.importSigningKeys({} as never)).rejects.toThrow();});
it('clears maintenance keys independently',async()=>{const p=memoryPrivateState();await p.setSigningKey('a','key');expect(await p.getSigningKey('a')).toBe('key');await p.removeSigningKey('a');expect(await p.getSigningKey('a')).toBeNull();await p.setSigningKey('a','key');await p.clearSigningKeys();expect(await p.getSigningKey('a')).toBeNull();});
it('refuses missing private witnesses',()=>{const context={privateState:{}} as Parameters<typeof witnesses.authoritySecret>[0];expect(()=>witnesses.authoritySecret(context)).toThrow('SR_UNAUTHORIZED');expect(()=>witnesses.credentialOpening(context)).toThrow('SR_INVALID');});
