import type {PrivateStateProvider} from '@midnight-ntwrk/midnight-js-types';
import type {SigningKey} from '@midnight-ntwrk/compact-runtime';
import type {ProductOpening,Witnesses} from '../../../contract/generated/contract/index.js';
export type PrivateState={authoritySecret?:Uint8Array;opening?:ProductOpening};
export const witnesses:Witnesses<PrivateState>={authoritySecret:({privateState})=>{if(!privateState.authoritySecret)throw new Error('SR_UNAUTHORIZED');return[privateState,privateState.authoritySecret];},credentialOpening:({privateState})=>{if(!privateState.opening)throw new Error('SR_INVALID');return[privateState,privateState.opening];}};
export function memoryPrivateState():PrivateStateProvider<string,PrivateState>{let address='';const states=new Map<string,PrivateState>();const keys=new Map<string,SigningKey>();const unsupported=async():Promise<never>=>{throw new Error('Private-state backup is unavailable; export the credential file locally.');};return{setContractAddress(value){address=value;},async set(id,value){states.set(address+':'+id,value);},async get(id){return states.get(address+':'+id)??null;},async remove(id){states.delete(address+':'+id);},async clear(){states.clear();},async setSigningKey(id,value){keys.set(id,value);},async getSigningKey(id){return keys.get(id)??null;},async removeSigningKey(id){keys.delete(id);},async clearSigningKeys(){keys.clear();},exportPrivateStates:unsupported,importPrivateStates:unsupported,exportSigningKeys:unsupported,importSigningKeys:unsupported};}
